<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navbar.ejs') %>
        <main class="centered">
            <h1><%= prods.title %></h1>
            <hr>
            <div class="detailImg ">
                <img src="/<%= prods.imageUrl %>" alt="<%= prods.imageUrl %>">
            </div>
            <h2><%= prods.price %></h2>
            <p><%= prods.description %></p>
            <% if(isAuthenticated) { %>
            <%- include('../includes/add-to-cart.ejs', {products: prods}) %>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>